name: Defang

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      action:
        description: "Whether to deploy up or down"
        required: true
        default: "up"
        type: choice
        options:
          - up
          - down
      stack:
        description: "The stack to deploy up or down. (Leave blank for default)"
        default: ""

jobs:
  defang:
    name: Defang ${{ github.event.inputs.action || 'up' }} ${{
      github.event.inputs.stack || 'default stack' }}
    environment: defang-production
    runs-on: ubuntu-latest
    timeout-minutes: 70
    permissions:
      contents: read
      id-token: write

    concurrency:
      cancel-in-progress: false
      group: deploy-${{ github.event.inputs.stack || 'default' }}

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Defang ${{ github.event.inputs.action || 'up' }} ${{
          github.event.inputs.stack || 'default stack' }}
        uses: DefangLabs/defang-github-action@v1.4.0
        with:
          command: ${{ github.event.inputs.action || 'up' }}
          stack: ${{ github.event.inputs.stack || '' }}
          config-vars-init-random: NC_S3_ACCESS_SECRET
          config-env-vars: NC_DB NC_S3_ACCESS_KEY NC_S3_BUCKET_NAME NC_S3_ENDPOINT NC_S3_REGION
        env:
          NC_S3_ACCESS_SECRET: ${{ secrets.NC_S3_ACCESS_SECRET }}
          NC_DB: ${{ secrets.NC_DB }}
          NC_S3_ACCESS_KEY: ${{ secrets.NC_S3_ACCESS_KEY }}
          NC_S3_BUCKET_NAME: ${{ secrets.NC_S3_BUCKET_NAME }}
          NC_S3_ENDPOINT: ${{ secrets.NC_S3_ENDPOINT }}
          NC_S3_REGION: ${{ secrets.NC_S3_REGION }}

      - name: Deployment Summary
        uses: DefangLabs/defang-github-action@v1.4.0
        with:
          command: services
          stack: ${{ github.event.inputs.stack || '' }}
